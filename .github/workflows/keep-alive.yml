name: Keep Alive

on:
  schedule:
    - cron: '*/14 * * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl https://botolink-web.onrender.com
      - run: curl https://easyvietnam-c7b6.onrender.com
